{
  "name": "function_arguments",
  "version": "0.0.0",
  "description": "Node.js Addons Example #2",
  "main": "addon.js",
  "private": true,
  "dependencies": {
    "bindings": "~1.2.1",
    "node-addon-api": "^1.0.0"
  },
  "scripts": {
    "test": "node addon.js",
    "configure": "node-gyp configure",
    "build": "node-gyp build",
    "clean": "node-gyp clean",
    "electron": "node-gyp rebuild --target=11.0.1 --arch=x64 --dist-url=https://atom.io/download/atom-shell",
    "rebuild": "node-gyp rebuild"
  },
  "gypfile": true,
  "devDependencies": {
    "electron-rebuild": "^2.3.5"
  }
}
